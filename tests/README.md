# Tests

Automated tests are a V3 emphasis (with earlier unit tests as components land).

Expected coverage later: SQL safety (read-only), semantic definitions, validators, refusal behaviour, data generators.
